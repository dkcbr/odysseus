{
  "name": "rapid_holdings_stress",
  "description": "Stress-tests the holdings-correction path (src/agent_loop.py's holdings-claim verification, and its interaction with the ticker LoRA's dedup-buffer fix, 16422cca) more aggressively than the default scenario: back-to-back real DK holdings with no plain, non-holding ticker between them, to check the fix holds up under repeated, consecutive triggering rather than a single occurrence per conversation. All turns use real, in-training tickers DK actually holds, so expect_tool_call: true is asserted throughout except the genuine follow-up.",
  "turns": [
    {"type": "ticker", "symbol": "SOUN", "expect_tool_call": true, "note": "real DK holding"},
    {"type": "ticker", "symbol": "KTOS", "expect_tool_call": true, "note": "real DK holding, immediately after another holdings-correction trigger"},
    {"type": "ticker", "symbol": "SOUN", "expect_tool_call": true, "note": "same holding asked again, later in the same conversation"},
    {"type": "followup", "message": "And KTOS, remind me?", "expect_tool_call": false, "note": "a real, short follow-up mixing an earlier holding by name -- should be answerable from context"},
    {"type": "ticker", "symbol": "KTOS", "expect_tool_call": true, "note": "final repeat -- third real DK holding reference in one conversation"}
  ]
}
